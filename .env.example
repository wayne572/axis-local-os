# Axis Local OS - hosted model adapter keys
#
# Copy this file to `.env` (gitignored) and fill in real values.
# Keys are loaded at process start by tools/local_os/model_runtime.py.
# They are never written to audit logs, configs, or stdout.

# Anthropic Claude
ANTHROPIC_API_KEY=

# OpenAI
OPENAI_API_KEY=
