# Normalise all text files to LF in the repo.
# Working trees on Windows still see CRLF via Git's autocrlf.
* text=auto eol=lf

# Treat these as binary - never normalise.
*.png binary
*.jpg binary
*.jpeg binary
*.pdf binary
*.zip binary
*.ico binary
*.webp binary
